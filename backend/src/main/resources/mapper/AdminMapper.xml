<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.vaccine.mapper.AdminMapper">

    <update id="update">
        UPDATE admin
        <set>
            <if test="realName != null">real_name = #{realName},</if>
            <if test="phone != null">phone = #{phone},</if>
            <if test="email != null">email = #{email},</if>
            <if test="avatar != null">avatar = #{avatar},</if>
            <if test="role != null">role = #{role},</if>
            <if test="communityId != null">community_id = #{communityId},</if>
            <if test="status != null">status = #{status},</if>
        </set>
        WHERE id = #{id}
    </update>

    <select id="findList" resultType="com.vaccine.entity.Admin">
        SELECT a.*, c.name as community_name FROM admin a
        LEFT JOIN community c ON a.community_id = c.id
        <where>
            <if test="username != null and username != ''">AND a.username LIKE CONCAT('%', #{username}, '%')</if>
            <if test="realName != null and realName != ''">AND a.real_name LIKE CONCAT('%', #{realName}, '%')</if>
            <if test="role != null and role != ''">AND a.role = #{role}</if>
            <if test="communityId != null">AND a.community_id = #{communityId}</if>
            <if test="status != null">AND a.status = #{status}</if>
        </where>
        ORDER BY a.create_time DESC
    </select>

    <select id="count" resultType="java.lang.Long">
        SELECT COUNT(*) FROM admin a
        <where>
            <if test="username != null and username != ''">AND a.username LIKE CONCAT('%', #{username}, '%')</if>
            <if test="realName != null and realName != ''">AND a.real_name LIKE CONCAT('%', #{realName}, '%')</if>
            <if test="role != null and role != ''">AND a.role = #{role}</if>
            <if test="communityId != null">AND a.community_id = #{communityId}</if>
            <if test="status != null">AND a.status = #{status}</if>
        </where>
    </select>
</mapper>
